# Everything MD


Use the fields below to upload your add-on package. After upload, a series of automated validation tests will be run on your file. The maximum file size accepted is 200.0 MB. If your add-on is larger than 200.0 MB, it will fail validation. 


npm install --global web-ext
web-ext build